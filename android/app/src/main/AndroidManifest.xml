<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by flutter_local_notifications to schedule exact-time
         reminders on Android 12+ (API 31+). Without these the system
         silently drops scheduled alarms and the "Alarms & reminders"
         toggle in Settings is rendered disabled. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Android 13+ (API 33+) — pre-granted use of exact alarms for the
         "Alarms & reminders" category. SCHEDULE_EXACT_ALARM alone only
         surfaces the user-toggle flow; USE_EXACT_ALARM is what
         auto-grants on fresh installs of the release APK. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- Re-arms scheduled notifications after a reboot. Required by
         flutter_local_notifications' ScheduledNotificationBootReceiver. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Required on Android 13+ (API 33+) to actually POST a notification
         to the user. The runtime permission is requested via
         `requestNotificationsPermission()` in NotificationService, but the
         manifest entry must be present for the OS to allow the dialog. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="fitness_diary"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Receivers used by flutter_local_notifications to deliver
             scheduled notifications and to re-arm alarms after a device
             reboot. Must be declared by the host app on Android 12+ -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
